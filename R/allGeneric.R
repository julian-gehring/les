##################################################
## generics for class 'Les'
##################################################

setGeneric("ci",
           function(object, ...) {standardGeneric("ci")}
           )

setGeneric("estimate",
           function(object, ...) {standardGeneric("estimate")}
           )

setGeneric("regions",
           function(object, ...) {standardGeneric("regions")}
           )

setGeneric("cutoff",
           function(object, ...) {standardGeneric("cutoff")}
           )

setGeneric("exportLambda",
           function(object, ...) {standardGeneric("exportLambda")}
           )

setGeneric("optimalKernel",
           function(object, ...) {standardGeneric("optimalKernel")}
           )

